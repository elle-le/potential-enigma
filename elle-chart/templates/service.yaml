apiVersion: v1
kind: Service
metadata:
  name: {{ include "elle-chart.fullname" . }}
  labels:
    {{- include "elle-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "elle-chart.selectorLabels" . | nindent 4 }}
